# Runtime dependencies for the API. Deliberately small.
#
# scikit-learn is NOT here. It pulls in scipy, and the three together are about
# 200MB unpacked, which puts the serverless function past Vercel's 225MB limit.
# The Isolation Forest is implemented on numpy in model/iforest.py and checked
# against scikit-learn in tests/test_iforest.py.
#
# Pinned to versions with prebuilt wheels for CPython 3.12 through 3.14.
fastapi==0.141.1
pydantic==2.13.5
numpy==2.5.2
python-multipart==0.0.32
