import { describe, it, expect } from 'vitest'

describe('Frontend test infrastructure', () => {
  it('vitest runs', () => {
    expect(true).toBe(true)
  })

  it('basic math works', () => {
    expect(2 + 2).toBe(4)
  })
})