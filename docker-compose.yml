services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - APP_ENV=development
      - LLM_PROVIDER=offline
      - VECTOR_STORE=memory
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
